# Build artifacts
build/
native/clap/build/
*.so
*.dylib
*.clap

# tone_bench transient outputs (baseline.json is committed)
native/clap/bench/last_run.json
native/clap/bench/last_run.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Hydra / training outputs
outputs/
artifacts/
multirun/
wandb/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Local-only weights too big for git (we keep tone_bundle/ but not arbitrary checkpoints)
*.ckpt
*.pt
!weights/static_*.pt

# Sonic harness binaries (sources tracked)
native/clap/bench/sonic/harness_*
!native/clap/bench/sonic/harness_*.cpp

# Coverage build tree (run_coverage.sh)
native/clap/build-cov/
